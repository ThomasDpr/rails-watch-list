<h1>Nouvelle liste</h1>

<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, 'Créer ma liste', class: 'btn btn-primary' %>
<% end %>
